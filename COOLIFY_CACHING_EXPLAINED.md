# Coolify 캐싱 및 재배포 전략 상세 가이드

## 🎯 핵심 요약

**Q: 재배포 시 전체 소스코드가 다시 업로드되나요?**
**A: 아니요! 변경된 파일만 Git pull로 가져옵니다.** ✅

---

## 1️⃣ Git 기반 증분 업데이트

### 최초 배포
```bash
# Coolify 서버에서 실행
git clone https://github.com/myaji35/12_InsureGraph-Pro.git
→ 전체 레포지토리 clone (~100MB)
```

### 재배포 (변경사항 발생 시)
```bash
# Coolify 서버에서 실행
git pull origin main
→ 변경된 파일만 다운로드 (~수 KB ~ 수 MB)
```

**예시**:
```bash
# 하나의 파일만 수정한 경우
Updating faabb34..a0d3b4d
Fast-forward
 frontend/src/app/dashboard/page.tsx | 10 +++++-----
 1 file changed, 5 insertions(+), 5 deletions(-)

→ 실제 전송량: ~5KB
```

---

## 2️⃣ Docker 레이어 캐싱 메커니즘

### Docker 레이어란?

Docker는 이미지를 여러 **레이어**로 나누어 관리합니다. 각 레이어는 독립적으로 캐싱됩니다.

### Frontend Dockerfile 레이어 분석

```dockerfile
# ============================================
# 레이어 1: Base Image (거의 안 바뀜)
# ============================================
FROM node:18-alpine AS deps
→ 캐시 키: node:18-alpine (이미지 해시)
→ 변경 확률: 0.1%
→ 크기: ~50MB

# ============================================
# 레이어 2: Package Files (가끔 바뀜)
# ============================================
WORKDIR /app
COPY package.json package-lock.json ./
→ 캐시 키: package.json + package-lock.json 파일 해시
→ 변경 확률: 5%
→ 크기: ~2KB

# ============================================
# 레이어 3: Dependencies (레이어 2가 안 바뀌면 캐시됨)
# ============================================
RUN npm ci --only=production
→ 캐시 키: 이전 레이어 해시 + 명령어
→ 변경 확률: 5% (package.json 변경 시에만)
→ 크기: ~300MB
→ 소요 시간: 3-5분

# ============================================
# 레이어 4: Source Code (자주 바뀜)
# ============================================
FROM node:18-alpine AS builder
COPY --from=deps /app/node_modules ./node_modules
COPY . .
→ 캐시 키: 소스 파일들 해시
→ 변경 확률: 90%
→ 크기: ~50MB

# ============================================
# 레이어 5: Build (레이어 4가 바뀌면 재실행)
# ============================================
RUN npm run build
→ 캐시 키: 이전 레이어 해시 + 명령어
→ 변경 확률: 90%
→ 소요 시간: 2-3분

# ============================================
# 레이어 6: Runtime (레이어 5가 바뀌면 재생성)
# ============================================
FROM node:18-alpine AS runner
COPY --from=builder /app/.next/standalone ./
→ 최종 이미지 크기: ~100MB
```

---

## 3️⃣ 재배포 시나리오별 상세 분석

### 📝 시나리오 A: UI 코드만 수정 (일반적인 경우)

**변경 사항**:
```bash
# 1개 파일만 수정
vi frontend/src/app/dashboard/page.tsx
```

**Coolify 재배포 프로세스**:

| 단계 | 동작 | 캐시 사용 | 시간 | 데이터 전송 |
|-----|------|----------|------|------------|
| 1. Git Pull | 변경 파일 다운로드 | N/A | 1초 | 5KB |
| 2. Layer 1 | Base image | ✅ 캐시 | 0초 | 0 bytes |
| 2. Layer 2 | Package files | ✅ 캐시 | 0초 | 0 bytes |
| 3. Layer 3 | npm install | ✅ 캐시 | 0초 | 0 bytes |
| 4. Layer 4 | Copy source | ❌ 재실행 | 5초 | 50MB (로컬) |
| 5. Layer 5 | npm run build | ❌ 재실행 | 1-2분 | 0 bytes |
| 6. Layer 6 | Runtime copy | ❌ 재실행 | 10초 | 100MB (로컬) |
| 7. 컨테이너 재시작 | Rolling update | N/A | 10초 | 0 bytes |

**총 소요 시간**: 1-3분
**네트워크 전송량**: ~5KB (Git pull만)
**로컬 디스크 I/O**: ~150MB (Docker 빌드)

---

### 📦 시나리오 B: NPM 패키지 추가

**변경 사항**:
```bash
npm install axios
→ package.json, package-lock.json 변경
```

**Coolify 재배포 프로세스**:

| 단계 | 동작 | 캐시 사용 | 시간 | 데이터 전송 |
|-----|------|----------|------|------------|
| 1. Git Pull | package.json 등 | N/A | 1초 | 10KB |
| 2. Layer 1 | Base image | ✅ 캐시 | 0초 | 0 bytes |
| 2. Layer 2 | Package files | ❌ 변경됨 | 1초 | 2KB |
| 3. Layer 3 | npm install | ❌ 재실행 | 3-5분 | ~5MB (인터넷) |
| 4. Layer 4 | Copy source | ❌ 재실행 | 5초 | 50MB (로컬) |
| 5. Layer 5 | npm run build | ❌ 재실행 | 2-3분 | 0 bytes |
| 6. Layer 6 | Runtime copy | ❌ 재실행 | 10초 | 100MB (로컬) |
| 7. 컨테이너 재시작 | Rolling update | N/A | 10초 | 0 bytes |

**총 소요 시간**: 5-8분
**네트워크 전송량**: ~5MB (npm install + git pull)
**로컬 디스크 I/O**: ~450MB

---

### ⚙️ 시나리오 C: 환경변수만 변경

**변경 사항**:
```bash
# Coolify UI에서 환경변수 수정
CORS_ORIGINS=http://58.225.113.125:18000,https://example.com
```

**Coolify 재배포 프로세스**:

| 단계 | 동작 | 캐시 사용 | 시간 | 데이터 전송 |
|-----|------|----------|------|------------|
| 1. Git Pull | 없음 | N/A | 0초 | 0 bytes |
| 2. Docker Build | 전체 스킵 | ✅ 캐시 | 0초 | 0 bytes |
| 3. 컨테이너 재시작 | 환경변수 주입 | N/A | 10초 | 0 bytes |

**총 소요 시간**: 10초
**네트워크 전송량**: 0 bytes
**로컬 디스크 I/O**: 0 bytes

---

### 🐍 시나리오 D: Backend Python 코드 수정

**변경 사항**:
```bash
vi backend/app/api/v1/endpoints/graph.py
```

**Coolify 재배포 프로세스**:

| 단계 | 동작 | 캐시 사용 | 시간 | 데이터 전송 |
|-----|------|----------|------|------------|
| 1. Git Pull | graph.py | N/A | 1초 | 5KB |
| 2. Layer 1 | python:3.11 | ✅ 캐시 | 0초 | 0 bytes |
| 2. Layer 2 | requirements.txt | ✅ 캐시 | 0초 | 0 bytes |
| 3. Layer 3 | pip install | ✅ 캐시 | 0초 | 0 bytes |
| 4. Layer 4 | Copy source | ❌ 재실행 | 2초 | 20MB (로컬) |
| 5. 컨테이너 재시작 | Rolling update | N/A | 5초 | 0 bytes |

**총 소요 시간**: 30초-1분
**네트워크 전송량**: ~5KB
**로컬 디스크 I/O**: ~20MB

**Python이 빠른 이유**: 컴파일 불필요 (인터프리터 언어)

---

## 4️⃣ Coolify 자동 배포 워크플로우

### GitHub Webhook 설정 시 자동화 흐름

```mermaid
개발자 로컬
    ↓ git push origin main
GitHub 레포지토리
    ↓ Webhook 트리거
Coolify 서버
    ↓ git pull (변경 파일만)
Docker 빌드 (캐싱 활용)
    ↓ 증분 빌드
새 컨테이너 시작
    ↓ Health check
기존 컨테이너 종료
    ↓ Zero downtime
배포 완료! 🎉
```

### 실제 로그 예시

```bash
# Coolify 배포 로그
[2025-12-07 10:15:30] 🔔 Webhook received from GitHub
[2025-12-07 10:15:31] 📥 Pulling latest changes...
[2025-12-07 10:15:32] ✅ Pulled 3 files (15KB)
[2025-12-07 10:15:32] 🐳 Starting Docker build...
[2025-12-07 10:15:33] ⚡ Using cached layer: node:18-alpine
[2025-12-07 10:15:33] ⚡ Using cached layer: npm ci
[2025-12-07 10:15:34] 🔄 Building changed files...
[2025-12-07 10:17:15] ✅ Build completed (1m 43s)
[2025-12-07 10:17:20] 🚀 Starting new container...
[2025-12-07 10:17:30] ✅ Health check passed
[2025-12-07 10:17:31] 🗑️ Stopping old container...
[2025-12-07 10:17:35] ✅ Deployment successful!
```

---

## 5️⃣ 캐싱 효율성 비교

### 최초 배포 vs 재배포

| 항목 | 최초 배포 | UI 코드 재배포 | 패키지 변경 재배포 |
|------|---------|--------------|----------------|
| **Git 다운로드** | 100MB | 5KB | 10KB |
| **Base Image** | 50MB 다운로드 | 캐시 (0 bytes) | 캐시 (0 bytes) |
| **Dependencies** | 300MB 설치 | 캐시 (0 bytes) | 5MB 추가 설치 |
| **Source Copy** | 50MB | 50MB | 50MB |
| **Build** | 2-3분 | 1-2분 | 2-3분 |
| **총 네트워크 전송** | ~450MB | ~5KB | ~5MB |
| **총 소요 시간** | 10-15분 | 1-3분 | 5-8분 |

### 캐싱 효과

```
최초 배포: ████████████████████ 100% (450MB, 15분)
UI 재배포: █                     5%  (5KB, 2분)
패키지 변경: ███                  15% (5MB, 7분)
```

**캐싱으로 인한 효율 향상**:
- 네트워크 전송량: **99% 감소** (450MB → 5KB)
- 빌드 시간: **80% 감소** (15분 → 2분)

---

## 6️⃣ 최적화 팁

### ✅ 이미 적용된 최적화

우리 프로젝트에 이미 적용되어 있습니다:

1. **Multi-stage Docker Builds**
   ```dockerfile
   FROM node:18-alpine AS deps    # 레이어 분리
   FROM node:18-alpine AS builder # 빌드 단계
   FROM node:18-alpine AS runner  # 최종 런타임
   ```

2. **Next.js Standalone Output**
   ```javascript
   // next.config.js
   output: 'standalone'  // 최소 크기 배포
   ```

3. **Dependencies 레이어 분리**
   ```dockerfile
   COPY package.json package-lock.json ./  # 먼저 복사
   RUN npm ci                              # 캐싱 가능
   COPY . .                                # 나중에 복사
   ```

### 🚀 추가 최적화 가능

#### .dockerignore 파일 추가

```bash
# .dockerignore (빌드 시 불필요한 파일 제외)
node_modules
.git
.github
.env.local
.next
out
coverage
*.log
README.md
*.md
.vscode
.idea
```

**효과**: 빌드 컨텍스트 크기 50% 감소

#### Turbo Cache 활용 (Monorepo인 경우)

```json
// package.json
{
  "scripts": {
    "build": "turbo build --cache-dir=.turbo"
  }
}
```

---

## 7️⃣ 실전 예시: 일주일 개발 워크플로우

### 월요일 (최초 배포)
```bash
git push origin main
→ 전체 빌드: 15분
```

### 화요일 (UI 수정 3회)
```bash
# 수정 1
git push origin main
→ 증분 빌드: 2분

# 수정 2
git push origin main
→ 증분 빌드: 2분

# 수정 3
git push origin main
→ 증분 빌드: 2분
```

### 수요일 (환경변수 변경)
```bash
# Coolify UI에서 수정
→ 재시작만: 10초
```

### 목요일 (패키지 추가)
```bash
npm install chart.js
git push origin main
→ Dependencies 재설치: 7분
```

### 금요일 (Backend 수정 2회)
```bash
# 수정 1
git push origin main
→ Python 빌드: 1분

# 수정 2
git push origin main
→ Python 빌드: 1분
```

**총 배포 시간**: 15분 + 6분 + 10초 + 7분 + 2분 = **약 30분**
**전통적 방식 (매번 전체 빌드)**: 15분 × 9회 = **135분**

**시간 절약**: **약 78% (105분 절약)** 🎉

---

## 8️⃣ 자주 묻는 질문 (FAQ)

### Q1: 캐시가 잘못되면 어떻게 하나요?

**A**: Coolify UI에서 "Clear Build Cache" 클릭
```bash
# 또는 Docker 명령어로
docker builder prune -a
```

### Q2: 첫 배포 후 변경사항 없이 재배포하면?

**A**: 모든 레이어 캐싱 + Git pull 없음 = **10초 만에 완료**

### Q3: 다른 브랜치로 배포하려면?

**A**: Coolify UI에서 Branch 변경
```
Settings → Branch → develop
→ git pull origin develop
```

### Q4: 로컬과 서버의 Docker 이미지는 다른가요?

**A**: 네, 독립적입니다.
- 로컬: 개발용 이미지
- Coolify 서버: 프로덕션 이미지 (별도 캐시)

---

## 9️⃣ 모니터링 및 디버깅

### Coolify 빌드 로그 확인

1. **Coolify Dashboard** → 프로젝트 선택
2. **Deployments** 탭
3. 최신 배포 클릭
4. **Logs** 탭

### 캐시 적중률 확인

```bash
# Coolify 로그에서 확인
[2025-12-07 10:15:33] ⚡ Using cached layer: node:18-alpine  ← 캐시 적중
[2025-12-07 10:15:34] 🔄 Building changed files...          ← 캐시 미스
```

### 빌드 시간 추적

| 배포 | 시간 | 캐시 사용 | 변경 사항 |
|-----|------|----------|----------|
| #1  | 15분 | 0%       | 최초 배포 |
| #2  | 2분  | 90%      | UI 수정 |
| #3  | 2분  | 90%      | UI 수정 |
| #4  | 7분  | 60%      | 패키지 추가 |
| #5  | 1분  | 95%      | Backend 수정 |

---

## 🎯 핵심 정리

### ✅ 재배포 시 업로드되는 것

1. **Git pull**: 변경된 파일만 (~수 KB ~ 수 MB)
2. **Docker 레이어**: 변경된 레이어만 재빌드
3. **Dependencies**: package.json 변경 시에만 재설치

### ❌ 재배포 시 업로드되지 않는 것

1. ~~전체 소스코드~~ (Git 캐싱)
2. ~~Base Docker 이미지~~ (레이어 캐싱)
3. ~~node_modules~~ (Dependencies 캐싱)
4. ~~이전 빌드 결과~~ (빌드 캐싱)

### 📊 효율성 지표

- **네트워크 전송 감소**: 99% ↓ (450MB → 5KB)
- **빌드 시간 단축**: 80% ↓ (15분 → 2분)
- **디스크 I/O 감소**: 70% ↓ (450MB → 150MB)
- **다운타임**: 0초 (Rolling update)

---

**작성일**: 2025-12-07
**버전**: 1.0
**프로젝트**: InsureGraph Pro
**대상 서버**: 58.225.113.125 (Coolify)
